# تقرير التدقيق التقني الشامل — maintenanceguide.life

**تاريخ التدقيق:** 23 أغسطس 2026  
**نطاق التدقيق:** React/Vite، TypeScript، المصادقة والصلاحيات، Supabase، الإشعارات وOneSignal، PWA، الأرشفة والتحصيل، الأداء، وبنية النشر.

## الخلاصة التنفيذية

المشروع قابل للبناء والإخراج الإنتاجي؛ فقد نجح `pnpm build`، كما أن شجرة Git نظيفة وقت التدقيق. لكن هذا النجاح لا يعني أن المشروع خالٍ من الأخطاء: فحص TypeScript (`pnpm check`) يفشل في **59 خطأً**، ولا توجد ملفات اختبار تلقائية، ووجد التدقيق مخاطر عالية في نموذج المصادقة والصلاحيات لأن الدخول يعتمد على قراءة كلمة المرور من جدول Supabase داخل المتصفح وتخزين صلاحية المستخدم في `localStorage`.

أعلى أولوية ليست تجميل الواجهة، بل **إغلاق حدّ الثقة الأمني** بين المتصفح وقاعدة البيانات، ثم إصلاح أخطاء TypeScript، ثم توحيد منظومة الإشعارات وإزالة المسارات القديمة. لا أوصي بإضافة ميزات مالية جديدة قبل معالجة هذه النقاط، خصوصاً لأن تأكيد التحصيل والحذف النهائي عمليات حساسة.

## الحالة القابلة للقياس

| الفحص | النتيجة | الدلالة |
|---|---:|---|
| `pnpm build` | ناجح | يمكن توليد حزمة إنتاج، مع تحذير حجم حزم كبير. |
| `pnpm check` | فشل — 59 خطأ TypeScript | البناء لا يتحقق من سلامة الأنواع، وقد توجد أخطاء مستقبلية غير ظاهرة في bundling. |
| Vitest | لا توجد ملفات اختبار | لا يوجد اختبار آلي يغطي الصلاحيات أو التحصيل أو الإشعارات. |
| `git diff --check` | ناجح | لا توجد مشاكل مسافات/أسطر في التغييرات الحالية. |
| حالة Git | نظيفة | لا توجد تغييرات غير ملتزم بها وقت القياس. |

## النتائج الحرجة

### 1. المصادقة والصلاحيات تعتمد على المتصفح — **حرج**

في `client/src/pages/LoginPage.tsx`، يتم طلب سجلات المستخدمين والفنيين من REST API ثم مقارنة `data[0].password === password` داخل المتصفح (الأسطر 31–41 و85–93). بعد ذلك تُحفظ بيانات المستخدم والدور في `localStorage` (الأسطر 48–58 و100–108). وفي `client/src/utils/authSession.ts` تتم قراءة هذه القيم باعتبارها جلسة موثوقة دون رمز جلسة أو تحقق من الخادم.

هذا يعني أن الصلاحية المعروضة في الواجهة قابلة للتعديل من أدوات المطور، كما أن كلمات المرور تُرسل إلى المتصفح ضمن استجابة المستخدمين. إخفاء زر تأكيد التحصيل لمدير العمليات ليس حماية كافية إذا كانت عمليات REST تسمح بالتعديل من العميل أو إذا أمكن تزوير `userRole`.

**الإجراء المطلوب:** نقل تسجيل الدخول إلى Supabase Auth أو نقطة خادم موثوقة، تخزين جلسة قصيرة العمر، تطبيق Row Level Security، وتنفيذ تأكيد التحصيل والحذف النهائي عبر دوال خادم/قاعدة بيانات تتحقق من الدور فعلياً.

### 2. كلمات المرور ظاهرة في لوحة الإدارة — **حرج**

في `ProtectedOrders.tsx` تُعرض قيمة `user.password` مباشرة داخل بطاقة إدارة المستخدمين (الأسطر 107–111)، كما يوجد حقل كلمة مرور ظاهر النص لحسابات المستخدمين في موضع آخر من الملف. هذا يكشف أسرار الحسابات لأي شخص يصل إلى الشاشة أو إلى بيانات المستخدمين.

**الإجراء المطلوب:** عدم تخزين كلمات المرور كنص صريح، عدم إرجاعها للواجهة، واستبدالها بتدفق تغيير كلمة مرور لا يعرض القيمة القديمة.

### 3. مسارات REST حساسة مكشوفة من العميل — **مرتفع**

عدة مكونات تستخدم مفتاح Supabase العام مباشرة لتنفيذ عمليات قراءة وتعديل وحذف على `orders` و`cash_ledger` و`users` و`notifications`. أمثلة واضحة موجودة في `ProtectedOrders.tsx` و`LoginPage.tsx` و`TechnicianPortal.tsx`. أمان هذه العمليات يعتمد بالكامل على سياسات Supabase غير الظاهرة في الكود؛ وإذا كانت سياسات RLS غير صارمة فسيستطيع أي عميل تجاوز الواجهة.

**الإجراء المطلوب:** مراجعة سياسات RLS فعلياً، منع التعديل والحذف المباشر من المتصفح للعمليات الحساسة، وإضافة قيود قاعدة بيانات تمنع تكرار قيد الربح وتتحقق من هوية المنفذ.

## نتائج الإشعارات وPWA

### 4. منظومتان أو أكثر للإشعارات — **مرتفع**

المسار النشط يستخدم OneSignal عبر `api/send-push.ts`، بينما توجد منظومة FCM في `server/index.ts` و`server/fcmService.ts` و`client/src/utils/fcm.ts`، إضافة إلى abstraction قديم في `client/src/services/notificationService.ts` يشير إلى مسارات Netlify غير المتطابقة مع النشر الحالي. هذا التعدد يفسر صعوبة تحديد سبب التأخير أو غياب الصوت، ويزيد احتمال أن يتم إصلاح مسار بينما يستمر مسار آخر في العمل.

`api/send-push.ts` يرسل محاولات متعددة عند فشل المصادقة، ويستخدم `ttl: 120` و`web_push_require_interaction: true`، لكن لا توجد آلية تسليم موثوقة داخل التطبيق لتسجيل الإرسال والاستلام ووقت الوصول. كما أن `sendExternalPush` يعيد نجاح الطلب بمجرد نجاح HTTP، دون تأكيد أن OneSignal سلّم الإشعار للجهاز.

**الإجراء المطلوب:** اعتماد OneSignal كمسار وحيد، حذف أو عزل FCM وNetlify القديمين، تسجيل `notification_id` ووقت الإرسال والنتيجة، وإضافة شاشة تشخيص تعرض permission/subscription/last delivery.

### 5. إعادة ضبط الإشعارات مدمرة أكثر من اللازم — **مرتفع**

في `NotificationStatus.tsx`، زر Nuclear Reset يلغي تسجيل **كل** Service Workers ويحذف كل مفتاح LocalStorage يحتوي على `onesignal` أو `OneSignal`، ثم يعيد تحميل الصفحة. هذا قد يزيل Worker أو بيانات تخص تطبيقاً آخر على نفس الأصل، ويؤدي إلى تسجيل خروج أو فقد إعدادات مؤقتة.

كما أن المكوّن يفحص الحالة كل 3 ثوانٍ، وهو polling زائد في واجهة الهاتف، ودليل المساعدة ما زال يذكر Firefox وTelegram رغم أن Telegram أُلغي من المتطلبات السابقة.

**الإجراء المطلوب:** حذف reset الشامل، حصر الإلغاء في Worker/مفتاح التطبيق الحالي، استبدال polling بمراقبة أحداث OneSignal عند توفرها، وتحديث تعليمات المساعدة.

### 6. Service Worker يستخدم استراتيجية Cache First للأصول — **متوسط إلى مرتفع**

في `client/public/sw.js`، يتم تقديم ملفات `/assets/` المخزنة قبل طلب الشبكة. تغيير `CACHE_NAME` يساعد عند النشر، لكنه لا يضمن دائماً وصول Worker الجديد بسرعة، وقد تظهر حزمة قديمة إذا لم تتم دورة التحديث كاملة. كما أن `APP_SHELL` يضم Manifest الموظفين داخل Worker مشترك مع الموقع العام.

**الإجراء المطلوب:** استخدام filenames الموقعة من Vite مع Network First للـHTML وStale-While-Revalidate للأصول، وإظهار رقم build فعلي من `import.meta.env` بدلاً من تحديث النص يدوياً في ملفات متعددة.

## نتائج TypeScript والبناء

### 7. فحص TypeScript يفشل في 59 موضعاً — **مرتفع**

الأخطاء موزعة على `ProtectedOrders.tsx` و`InvoicePage.tsx` و`PickupReceiptPage.tsx` و`TechnicianPortal.tsx` وصفحات الخدمات. أبرزها: معاملات `implicit any` في حسابات الخزنة، `fetchpriority` بدلاً من `fetchPriority` في عدة صفحات، خصائص nullable تُمرر إلى واجهات تتطلب `string`، دالة `deletePartner` غير المعرفة، ونوع `PickupType` غير المتطابق.

نجاح Vite لا يلغي هذه الأخطاء، لأن bundler يستطيع تمرير بعض أخطاء الأنواع. لذلك لا ينبغي اعتبار الإصدار مستقراً بالكامل قبل جعل `pnpm check` ينجح.

### 8. لا توجد اختبارات آلية — **مرتفع**

لم يجد Vitest أي ملف `*.test.*` أو `*.spec.*`. لا يوجد حالياً اختبار يغطي: منع مدير العمليات من تأكيد التحصيل، منع التكرار في الخزنة، عدم رنين الأوردر القديم، فتح الإشعار على الأوردر، الأرشفة بعد 30 يوماً، أو فصل صفحة الزوار عن الموظفين.

**الإجراء المطلوب:** إضافة اختبارات وحدات للدوال المالية والتوقيت، واختبارات مكونات لمسارات الصلاحيات والإشعارات، ثم اختبار متصفح واحد على الأقل لمسار الدخول والتأكيد.

## نتائج مالية وتشغيلية

### 9. علامة الإنهاء اليدوي داخل `technician_note` حل مؤقت — **مرتفع**

الإجراء الحالي لإخفاء التحصيلات القديمة يضيف النص `[COLLECTION_MANUAL_CLOSED]` داخل ملاحظات الفني. هذا يخلط بيانات التشغيل مع حالة مالية، وقد يتأثر إذا قام مستخدم بتحرير الملاحظة أو إذا تغير شكل النص. كما أن الإخفاء لا يثبت أن العميل دفع، بل يزيل الأوردر من قائمة التأكيد فقط؛ وهذه نقطة صحيحة من ناحية السلامة لكنها تحتاج تسمية واضحة حتى لا يختلط «مغلق يدوياً» مع «محصل ومثبت في الخزنة».

**الإجراء المطلوب:** إضافة أعمدة صريحة مثل `collection_status`, `collection_closed_at`, `collection_closed_by`, و`collection_method`, مع قيمة `manual_external` منفصلة عن `is_paid`. يجب ألا يزيد هذا الإجراء الخزنة أو يضيف ربحاً تلقائياً.

### 10. خطر تضارب التاريخ والتوقيت — **مرتفع**

يحتوي `ProtectedOrders.tsx` على `REPORT_TIME_OFFSET_MS = 8 ساعات`، بينما يحتوي المشروع أيضاً على أدوات توقيت القاهرة. إضافة ساعات ثابتة إلى timestamp قد تنتج خطأً عند مقارنة UTC أو عند اختلاف التوقيت الصيفي، وتؤثر في الأرشفة والتأخير ونسب التقارير.

**الإجراء المطلوب:** اعتماد ISO/UTC في قاعدة البيانات، واستخدام `Intl.DateTimeFormat` مع `Africa/Cairo` للعرض فقط، ومنع أي offset ثابت. يجب إضافة اختبارات حول منتصف الليل وتغيير اليوم.

### 11. التحديث الجماعي للتصفية ليس ذرياً بالكامل — **متوسط إلى مرتفع**

توجد عمليات `Promise.all` لتحديث عدة أوردرات أو إضافة قيود مالية. إذا نجح جزء وفشل جزء، فقد تعرض الواجهة رسالة عامة دون سجل دقيق للعمليات التي نجحت. هذا مهم في التصفية الجماعية والحذف الجماعي.

**الإجراء المطلوب:** نقل العمليات الجماعية الحساسة إلى RPC/transaction في قاعدة البيانات، أو تنفيذها مع سجل نتائج لكل أوردر وواجهة retry واضحة.

## نتائج الأداء

### 12. حجم الحزم كبير — **متوسط**

يُظهر البناء تحذيراً بأن بعض الحزم تتجاوز 500KB بعد minification، ومنها `TechnicianPortal` وملف الدخول الرئيسي. وجود `lazy` للمسارات جيد، لكن المكونات الكبيرة ما زالت تحمل أعمالاً كثيرة داخل ملف واحد، ويظهر في المشروع تكرار لواجهات وأدوات قديمة.

**الإجراء المطلوب:** تقسيم `ProtectedOrders` و`TechnicianPortal` إلى وحدات، تحميل PDF/html2canvas عند طلب الفاتورة فقط، إزالة المكونات غير المستخدمة، وتقليل الاعتماد على `any` الذي يخفي فرص التحسين.

### 13. ازدواجية مكونات وواجهات قديمة — **متوسط**

توجد نسخ غير مستخدمة أو موازية مثل `ProtectedOrdersEnhanced.tsx` و`AdvancedOrdersManagement.tsx` و`OrderCard.tsx` و`BookingFormEnhanced.tsx`، إضافة إلى أنظمة إشعارات قديمة. وجود هذه الملفات لا يكسر التشغيل مباشرة لكنه يرفع كلفة الصيانة ويزيد احتمال تعديل الملف الخطأ.

**الإجراء المطلوب:** إنشاء سجل واضح للمكونات النشطة، نقل النماذج القديمة إلى مجلد `legacy` مؤقتاً، ثم حذفها بعد التأكد من عدم وجود imports أو روابط نشر تعتمد عليها.

## خطة الإصلاح المقترحة

| الأولوية | الإجراء | معيار القبول |
|---|---|---|
| P0 | إصلاح نموذج المصادقة وRLS وعدم إرسال كلمات المرور للمتصفح | لا يمكن تعديل `userRole` محلياً لتجاوز الصلاحية، ولا تظهر كلمات المرور في الاستجابات أو الواجهة. |
| P0 | حماية تأكيد التحصيل والحذف النهائي داخل الخادم/قاعدة البيانات | مدير النظام فقط ينجح في العملية حتى عند استدعاء REST مباشرة. |
| P1 | إصلاح أخطاء TypeScript | `pnpm check` ينجح بلا أخطاء. |
| P1 | توحيد OneSignal وإزالة المسارات القديمة | مسار إرسال واحد مع سجل إرسال وفشل قابل للتشخيص. |
| P1 | استبدال علامة الملاحظات بحالة مالية صريحة | لا تختلط حالة الإنهاء اليدوي بملاحظات الفني. |
| P1 | إضافة اختبارات مالية وإشعارات وصلاحيات | تغطية السيناريوهات الحرجة المذكورة أعلاه. |
| P2 | تحسين استراتيجية الكاش وتقسيم الحزم | انخفاض حجم الحزم الأولية وعدم ظهور نسخة قديمة بعد النشر. |
| P2 | إزالة الواجهات القديمة وتحسين بنية المكونات | لا توجد نسخ موازية غير مستخدمة لمسارات الإنتاج. |

## القرار الفني

**لا أوصي بإعلان المشروع «مستقراً 100%» حالياً** رغم نجاح البناء؛ لأن فحص الأنواع يفشل ولأن حماية الصلاحيات الحالية تعتمد على العميل. يمكن استمرار التشغيل المؤقت مع تقليل الوصول إلى لوحة الإدارة وتغيير كلمات المرور، لكن الإصدار التالي يجب أن يبدأ بإصلاح المصادقة وRLS قبل أي توسعة واجهة.

## أدلة التدقيق الداخلية

- `client/src/pages/LoginPage.tsx:31-41, 48-58, 85-108` — قراءة كلمات المرور في العميل وتخزين الدور محلياً.
- `client/src/utils/authSession.ts:11-30` — اعتبار LocalStorage جلسة موثوقة ومسح محلي فقط.
- `client/src/components/ProtectedOrders.tsx:107-111` — عرض كلمة المرور في واجهة الإدارة.
- `client/src/components/NotificationStatus.tsx:27-31, 33-61, 152-159` — polling متكرر وReset شامل وتعليمات قديمة.
- `client/public/sw.js:13-45` — استراتيجية الكاش الحالية.
- `api/send-push.ts:140-231` — معالجة OneSignal والأولوية وTTL.
- `server/index.ts:11-20, 50-79, 90-141` — مسار FCM موازٍ ومفاتيح الخادم.
- `/tmp/audit_tsc_final.log` — سجل فشل TypeScript بعدد 59 خطأً.
- `/tmp/audit_vitest.log` — لا توجد ملفات اختبار.

> هذا التقرير يميز بين ما تم التحقق منه مباشرة في الكود ونتائج تحتاج فحصاً في إعدادات Supabase/Vercel، مثل سياسات RLS الفعلية ومفاتيح البيئة.
