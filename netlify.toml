[build]
  command = "pnpm install --ignore-scripts && pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA Redirects - يحل مشكلة 404 للروابط الجديدة (login, tech-portal, data-entry)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
