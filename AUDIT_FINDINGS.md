# تقرير المراجعة الشاملة — نتائج أولية

## المرحلة المالية

### ملاحظات حرجة تحتاج إصلاحاً

1. `fetchCashLedger` في `ProtectedOrders.tsx` يجمع `entry.amount` مباشرة دون تحويله إلى رقم. إذا أعاد Supabase المبلغ كنص فقد يتحول الرصيد إلى concatenation نصي بدلاً من جمع حسابي.
2. `addCompanyProfitToCash` يعتمد على فحص واجهة المستخدم `profit_added_to_cash` قبل الإدراج، لذلك يمكن لطلبين متزامنين أن يضيفا نفس ربح الأوردر مرتين. يلزم مسار idempotent أو فحص/قيد فريد من جهة البيانات.
3. `addCompanyProfitToCash` يعتبر العملية ناجحة بمجرد نجاح POST لقيد الخزنة، ولا يتحقق بشكل صارم من نجاح PATCH الذي يضع `profit_added_to_cash=true`. فشل PATCH قد يسمح بتكرار الإضافة لاحقاً.
4. `deleteOrderProfitFromCash` يحذف قيود الدخل بالبحث النصي في الوصف، وهو حل هش؛ يجب وجود معرف مرجعي للأوردر في قيد الخزنة أو على الأقل تضييق المطابقة.
5. `confirmCompanyTransferReceipt` يغيّر `is_paid=true` قبل اكتمال إضافة القيد وتحديث وسم التحويل، ثم يحاول rollback جزئياً. يلزم التحقق من ردود PATCH واستخدام مسار ذري إن أمكن.
6. `handleBatchConfirmPaid` يحدّث `is_paid` للأوردرات دفعة واحدة لكنه لا يضيف أرباح الشركة للخزنة لكل أوردر، ولا يحدّث `profit_added_to_cash`; هذا قد يفصل حالة التحصيل عن الرصيد.
7. `updateOrderStatus` يبحث عن الأوردر داخل `orders` فقط، لذلك تعديل أوردر موجود في `archivedOrders` قد لا يعمل أو قد يرفض العملية بصمت.

### ملاحظات سلوكية تحتاج توحيداً

- بعض إشعارات النشاط تذهب إلى جدول `notifications` وبعضها إلى OneSignal فقط؛ يلزم توحيد سجل داخلي دائم لكل حركة.
- صلاحية التحصيل مقيدة بالمدير العام كما هو مطلوب، بينما التعديل التشغيلي لمدير العمليات يحتاج إبقاءه منفصلاً عن الخزنة.
- قاعدة الأرشفة الحالية تُبقي المكتمل غير المحصل في التشغيل بعد الإصلاح، وهو متوافق مع أولوية التحصيل.

## لم يتم تعديل المنطق المالي في هذه المرحلة قبل مراجعة بقية المسارات والاختبارات.
ابع المهمة: مراجعة الصلاحيات، الأرشفة والفلاتر، الإشعارات، والأداء ثم تطبيق إصلاحات آمنة مع اختبار البناء.

تاريخ المراجعة: 2026-08-22

## نتائج مراجعة OneSignal الرسمية

المصادر التي تمت مراجعتها:
- https://documentation.onesignal.com/docs/en/onesignal-service-worker
- https://documentation.onesignal.com/docs/en/web-sdk-reference
- https://documentation.onesignal.com/docs/en/troubleshooting-web-push

النتائج المؤكدة من التوثيق:

1. ملف `OneSignalSDKWorker.js` الموصى به من OneSignal يحتوي على سطر `importScripts` فقط. إضافة معالج `notificationclick` مخصص فوقه قد تتنافس مع معالج OneSignal المدمج، لذلك يجب اختباره أو إزالته والاكتفاء بإعداد `notificationClickHandlerAction` و`launchURL` عند الحاجة.
2. OneSignal يوصي بملف Service Worker مخصص منفصل، أو دمجه مع Service Worker آخر بطريقة موثقة. وجود `sw.js` آخر في المشروع يستحق التأكد من أنه لا يُسجّل بنفس النطاق؛ لا يجوز وجود Service Workerين نشطين لنفس النطاق.
3. التوثيق يعرّف `notificationClickHandlerMatch: 'origin'` على أنه يركز أي تبويب من نفس الأصل، و`notificationClickHandlerAction: 'navigate'` على أنه يتنقل إلى `launchURL`. هذا قد يفسر انتقال الإشعار بين تبويب المتصفح وPWA إذا كان الرابط أو النطاق غير ثابت.
4. اشتراط HTTPS، وإتاحة ملف Service Worker بدون تسجيل دخول، وContent-Type JavaScript، شروط أساسية.
5. على Android يجب التأكد من أن قناة إشعارات المتصفح ليست Silent. وعلى iOS يلزم تثبيت الموقع على الشاشة الرئيسية قبل وصول Push.
6. الوضع الخفي/الخاص لا يدعم الاشتراك في Push بشكل موثوق، ولذلك لا يصلح للاختبار.

ملاحظة مهمة: لا ينبغي اعتبار ظهور رسالة نجاح من API دليلاً على وصول الإشعار إلى جهاز محدد؛ يلزم تتبع نتيجة OneSignal ومعرّف المشترك وسجل داخلي للحركة.
