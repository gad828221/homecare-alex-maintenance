# حالة إصلاح نظام الإشعارات (Maintenance Guide) - v3.1.5

## المشاكل التي تم حلها:
1. **Chrome Android**: تم حل مشكلة تعليق زر التفعيل بإضافة "إعادة ضبط نووي" ومؤقت أمان، مع دليل يدوي لفك الحظر عبر علامة القفل.
2. **تفاصيل الرسالة**: تم التأكد من أن Firefox يظهر التفاصيل كاملة (اسم العميل، الجهاز) بنجاح.
3. **التوجيه**: تم تعديل محرك الإرسال (send-push.ts) ليرسل روابط دقيقة تنتهي بـ `?source=pwa`.

## المشكلة الحالية:
عند الضغط على الإشعار في Firefox، يفتح المتصفح تبويباً جديداً (صفحة الزوار/الدخول) بدلاً من فتح التطبيق المثبت (PWA)، وذلك لأن المستخدم غير مسجل دخول في المتصفح.

## الإجراء الحالي (v3.1.5):
1. تحديث `OneSignalSDKWorker.js` ليقوم بـ `client.navigate(targetUrl)` و `client.focus()` للنافذة المفتوحة بدلاً من فتح تبويب جديد.
2. تحديث الـ `manifest` لتعريف `id` ثابت للتطبيق لضمان ربط الروابط به.

## الخطوات القادمة:
- مراقبة استجابة الهاتف بعد تحديث الـ Service Worker.
- تحسين `App.tsx` لاكتشاف الدخول من إشعار والتحويل الفوري للأوردر.
